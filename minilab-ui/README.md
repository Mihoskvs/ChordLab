# MiniLab UI

Vite + React WebMIDI interface for sending SysEx updates to the Arturia MiniLab 3.

## Scripts

```bash
pnpm install
pnpm dev
```

## WebMIDI requirements

Chrome/Edge with `--enable-experimental-web-platform-features` and HTTPS context.
