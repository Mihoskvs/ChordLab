# ChordLab Assets

This folder stores reference material such as SVG pad layouts, MCC presets and
other blueprints mentioned in the project brief.  Drop-in files are ignored by
Git via the `.gitignore` entry, but this README keeps the directory tracked.
